[project]
name = "simple-news-site"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = ["django>=5.1.3", "python-dotenv>=1.0.1", "ruff>=0.7.4"]
